import { EnjinSidebarComponent } from './enjin-sidebar-component';

describe('enjin-sidebar-component', () => {
  it('should build', () => {
    expect(new EnjinSidebarComponent()).toBeTruthy();
  });
});